import { env } from "cloudflare:workers";
import { getDb } from "../../../../../db";
import { connections } from "../../../../../db/schema";
import { requireApiUser } from "../../../../server/current-user";
import { encryptSecret, verifyState } from "../../../../server/secrets";

type TokenResponse={access_token:string;refresh_token:string;expires_at:number;scope?:string;athlete:{id:number;firstname?:string;lastname?:string}};
export async function GET(request:Request){try{const user=await requireApiUser();const query=new URL(request.url).searchParams;const state=query.get("state")||"",code=query.get("code")||"";if(!code||!await verifyState(state,user.userId))return Response.json({error:"Invalid OAuth callback"},{status:400});const config=env as unknown as Record<string,string>;const response=await fetch("https://www.strava.com/oauth/token",{method:"POST",headers:{"content-type":"application/x-www-form-urlencoded"},body:new URLSearchParams({client_id:config.STRAVA_CLIENT_ID,client_secret:config.STRAVA_CLIENT_SECRET,code,grant_type:"authorization_code"})});if(!response.ok)return Response.json({error:"Strava rejected the token exchange"},{status:502});const token=await response.json() as TokenResponse;const now=new Date();await getDb().insert(connections).values({userId:user.userId,provider:"strava",providerUserId:String(token.athlete.id),displayName:[token.athlete.firstname,token.athlete.lastname].filter(Boolean).join(" "),scopes:query.get("scope")||token.scope||"",accessTokenEncrypted:await encryptSecret(token.access_token),refreshTokenEncrypted:await encryptSecret(token.refresh_token),tokenExpiresAt:new Date(token.expires_at*1000),connectedAt:now}).onConflictDoUpdate({target:[connections.userId,connections.provider],set:{providerUserId:String(token.athlete.id),displayName:[token.athlete.firstname,token.athlete.lastname].filter(Boolean).join(" "),scopes:query.get("scope")||token.scope||"",accessTokenEncrypted:await encryptSecret(token.access_token),refreshTokenEncrypted:await encryptSecret(token.refresh_token),tokenExpiresAt:new Date(token.expires_at*1000),connectedAt:now}});return Response.redirect(new URL("/?connected=strava",request.url))}catch(error){if(error instanceof Response)return error;return Response.json({error:"Unable to complete Strava connection"},{status:500})}}
