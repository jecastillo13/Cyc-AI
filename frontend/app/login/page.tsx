"use client";
import { FormEvent, useState } from "react";
export default function Login(){const [register,setRegister]=useState(false);const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const response=await fetch(`/api/auth/${register?"register":"login"}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(form))});const body=await response.json();if(response.ok)window.location.href="/";else{setError(body.error||"No fue posible continuar.");setBusy(false)}}return <main className="auth-page"><section className="auth-card"><div className="brand auth-brand"><span className="brand-mark">C</span><span>Cyc—AI</span></div><p className="eyebrow">Tu entrenamiento, en un solo lugar</p><h1>{register?"Crea tu cuenta":"Bienvenido"}</h1><p className="sub">Conecta tus aplicaciones deportivas y sincroniza automáticamente tus actividades.</p><form onSubmit={submit}>{register&&<label>Nombre<input name="displayName" minLength={2} required autoComplete="name"/></label>}<label>Correo<input name="email" type="email" required autoComplete="email"/></label><label>Contraseña<input name="password" type="password" minLength={8} required autoComplete={register?"new-password":"current-password"}/></label>{error&&<p className="form-error">{error}</p>}<button className="primary" disabled={busy}>{busy?"Espera…":register?"Crear cuenta":"Entrar"}</button></form><button className="auth-switch" onClick={()=>{setRegister(value=>!value);setError("")}}>{register?"Ya tengo una cuenta":"Crear una cuenta nueva"}</button></section></main>}
